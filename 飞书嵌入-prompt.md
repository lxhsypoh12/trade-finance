# 任务
修改现有 HTML 文件 `套利计算系统.html`，将三个本地业务统计表（投标/中标/完成）替换为飞书 Base 嵌入。

# 背景
- 已在飞书创建 Base「贸易业务统计」，包含一表三视图
- 每个视图按「阶段」字段筛选：投标阶段 / 中标阶段 / 完成阶段
- 用户稍后会提供飞书嵌入链接（一个 Base URL + 不同 view 参数）

# 修改范围（最小化改动，不影响计算器）

## 1. 首页入口卡片保持不变
三张统计卡片（投标阶段/中标阶段/完成阶段）保留，点击后仍跳转对应视图。

## 2. 三个统计视图内容替换
将视图5/6/7 的原有 `<table>` + JS CRUD 逻辑替换为 `<iframe>` 嵌入飞书：

```html
<div class="stats-iframe-wrapper">
  <iframe src="飞书嵌入链接" frameborder="0" allow="fullscreen"></iframe>
</div>
```

## 3. CSS 新增样式
- iframe 容器全宽自适应，高度撑满视口
- 保留「← 返回主页」页头

## 4. JS 清理
- 移除三个统计表的本地 CRUD 代码（新增行、删除行、计算汇总等）
- 保留视图切换函数 showView 中 stats-bid/stats-win/stats-done 分支，但改为纯显隐无逻辑

## 5. 不影响的部分（严格保留）
- ✅ 视图1 主页及其卡片
- ✅ 视图2/3 银行承兑汇票 + 信用证套利计算器
- ✅ 视图4 贸易金融业务报价计算器
- ✅ 所有计算器的输入/计算/显示/PDF 导出逻辑
- ✅ 现有 CSS 样式体系

# 输出要求
更新已有文件：`C:\Users\Lenovo\Desktop\AI\应用\贸易公司提效\套利计算系统.html`
在现有代码基础上精准替换，最小化改动量。
